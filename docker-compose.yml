services:
  postgres:
    image: postgres:16
    container_name: smartfleet-postgres
    environment:
      POSTGRES_DB: smartfleet
      POSTGRES_USER: smartfleet
      POSTGRES_PASSWORD: devpassword123
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: